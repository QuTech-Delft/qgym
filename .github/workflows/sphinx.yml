name: sphinx

on: push

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - name: Install dependencies
        run: |
          pip install sphinx sphinx_autodoc_typehints sphinx_math_dollar sphinx_rtd_theme
      - name: Sphinx build
        run: |
          sphinx-apidoc -o docs_build -f -F -M -e -t docs --implicit-namespaces python/qgym
          sphinx-build docs_build docs_html
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/master' }}
        with:
          publish_branch: gh-pages
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs_html/
          force_orphan: true